---
name: english-paper-pdf-chinese-translation
description: 将英文论文PDF完整翻译为中文。在保证内容、公式、参考文献、图片和数据正确的前提下，保持原论文的页面体系与版式风格，并针对中文重新流排，生成清晰、可读、可选择和可检索的中文PDF。适用于英文科技论文、学术论文、会议论文和期刊论文的中文翻译与高保真重排。
---

# 英文论文 PDF 中文翻译与高保真重排

请将用户提供的英文论文 PDF 完整翻译成中文，并生成一份保持原论文整体版式风格、但针对中文重新流排的中文 PDF。

目标不是在原 PDF 的固定文本框中机械替换英文，而是：

> 保持原论文的页面体系、单双栏结构、标题层级、图表公式及其逻辑对应关系，在此基础上按照中文长度和字体度量重新进行自然排版。

请直接执行完整任务，不要只描述方案。

---

## 输入

- 待翻译论文：用户提供的英文论文 PDF
- 中文排版参考：用户可选提供的参考文件

参考文件仅用于理解中文论文的排版风格，不得复制其中的译文、数据或其他内容。

---

## 输出

仅输出：

`[论文名]_中文翻译.pdf`

不要额外生成 Word、Markdown、TXT、排版检查报告或翻译说明。

质量检查仍必须在内部完整执行，但不单独输出检查报告。

---

## 一、优先级与冲突处理

当不同要求发生冲突时，按以下优先级处理。

### P0：绝对优先，不能牺牲

1. 内容完整准确，不得遗漏、重复、误译、裁切或遮挡。
2. 原文自然段与中文自然段一一对应，阅读顺序正确。
3. 数学公式的内容、编号和数学意义完全不变。
4. 参考文献区域保持原 PDF 原样。
5. 图片内容、表格数据、数值、单位、变量、实验结果和引用编号不得改变。
6. 不得为了保持表面版式而使用明显过小字号、极端压缩字距或其他降低可读性的方式。

### P1：强要求

1. 保持原论文页面尺寸和方向。
2. 保持原论文单栏或双栏结构、栏宽和栏间距风格。
3. 保持标题层级、图表编号、公式编号及其与正文的逻辑对应关系。
4. 保持原文与译文自然段的一一映射。
5. 中文正文应清晰、可读、可选择、可复制、可检索。
6. 中文字体、标点及中英文混排应符合本提示词。

### P2：尽量满足

1. 页数尽量接近原 PDF，但不强制一致。
2. 图片、公式、表格尽量位于原章节、原栏和相近视觉位置。
3. 字号、行距和局部间距尽量接近原论文的视觉风格。
4. 在不牺牲 P0 和 P1 的前提下，尽量避免无意义增加页数。

不得为了保持原页数、原对象绝对坐标或表面版式一致而牺牲内容完整性、清晰度或可读性。

---

## 二、自然段识别与翻译边界

“保持原文换行”是指：

> 原文一个自然段对应中文一个自然段。

不是让中文复制英文 PDF 中每一条视觉行的换行位置。

必须先根据真实页面版式重建英文自然段，再以完整自然段为单位翻译。

严格要求：

1. 不得按照 PDF 提取文本中的视觉行逐行翻译。
2. 删除由页面排版产生的普通行尾换行。
3. 中文段内只能由排版引擎根据栏宽自然换行。
4. 不得保留英文视觉行尾或人工插入句中硬换行。
5. 不得拆分、合并、遗漏、重复或调换自然段。
6. 标题、列表项、图注、表注和脚注分别作为独立翻译单元。
7. 双栏论文按照左栏从上到下、右栏从上到下、再进入下一页的顺序处理。
8. 必须正确续接左栏底部至右栏顶部、右栏底部至下一页以及被图片、表格或公式分隔的同一自然段。
9. 无法仅凭提取文本判断段落边界时，必须查看原 PDF 页面图像，不得猜测。

段落边界应综合判断：

- 行尾是否有明显留白；
- 下一行是否有首行缩进；
- 行间距是否变化；
- 字体、字号或样式是否变化；
- 句末标点和语义是否完整；
- 下一元素是否为标题、列表、公式、图注或表格；
- 是否发生换栏或换页。

“右侧留白”不得单独作为绝对判断依据。

---

## 三、英文行尾断词

若英文单词因换行被连字符拆开，应恢复完整单词。

例如，`reconfig-`与下一行的`urable`应恢复为`reconfigurable`。

但不得误删以下内容中的有效连字符或连接符：

- 正式复合词；
- 专有名称；
- 模型名称；
- 网络层名称；
- 引用范围；
- 数学区间；
- 负号和减号；
- 编号、DOI、URL 和单位表达式。

---

## 四、翻译质量与术语

1. 翻译必须准确、完整、专业、客观，符合中文学术论文表达习惯。
2. 不得采用摘要式翻译，不得删除限定条件，不得自行补充原文不存在的结论。
3. 必须准确保留因果、转折、条件、比较、程度、否定和不确定性。
4. 可以调整英文长句内部语序和中文分句方式，但不得改变原文技术含义或论证强度。
5. 建立全文术语表，同一术语必须统一译法。
6. 重要术语首次出现时可采用“中文名称（英文名称，缩写）”，例如：
   - 行驻留（Row Stationary，RS）
   - 处理单元（Processing Element，PE）
   - 全局缓冲区（Global Buffer，GLB）
   - 片上网络（Network-on-Chip，NoC）
   - 输入特征图（input feature map，ifmap）
   - 输出特征图（output feature map，ofmap）
   - 部分和（partial sum，psum）
7. 后续统一使用中文术语或该领域通常采用的缩写。
8. 公式、变量、模型名称、数据集名称、芯片名称、层名称、数值、单位和引用编号保持原样。
9. 对于业界和学术界通常直接使用英文的名称，不要强行中文化。

---

## 五、论文首页与元信息

论文首页及页眉页脚中的元信息按以下规则处理：

- 论文标题：翻译为中文。
- `Abstract`、摘要正文：翻译。
- `Keywords`、关键词内容：翻译；专业缩写和专有名称按术语规则处理。
- 作者姓名：保持原样，不翻译、不音译。
- 作者单位：翻译为中文；机构专有名称无法可靠翻译时可保留英文。
- 期刊名、会议名：保持原样。
- 卷、期、页码、文章编号：保持原样。
- DOI、URL：保持原样。
- 收稿、修回、录用、在线发表等说明性文字：翻译为中文，日期和数值保持原样。
- Copyright、出版商固定版权信息：保持原样，不翻译、不重排其核心标识内容。
- 通讯作者标记、电子邮箱：保持原样；“通讯作者”“电子邮箱”等说明文字可以翻译。

---

## 六、字体规则

### 中文字体

所有新生成的中文文字和中文全角标点优先使用宋体，包括：

- 标题；
- 正文；
- 摘要；
- 关键词；
- 图注；
- 表题；
- 表头和表内中文；
- 脚注；
- 作者简介。

若系统不存在可用宋体，可使用清晰、可嵌入且适合中文学术排版的 CJK 字体替代，不得因字体缺失而生成乱码、空白或不可选择文字。

### 英文和数字字体

所有新生成或重新排版的英文、拉丁字母、阿拉伯数字、英文缩写和单位优先使用 Times New Roman，例如：

- `CNN`
- `FPGA`
- `DRAM`
- `Eyeriss`
- `VGG-16`
- `CONV1–CONV5`
- `200 MHz`
- `65 nm`
- `33.6 GMAC/s`
- `[12]–[22]`

字体切换只能通过字符字体属性实现，不得通过插入空格实现。

输出 PDF 应嵌入字体或字体子集。中文正文必须可选择、复制和检索。

### 中文正文的字号与视觉大小

不得机械复制原英文正文的 point size，也不得为了把中文塞回原英文文本框而主动缩小字号。

处理规则：

1. 首先识别原英文论文正文的主要字号 `S_original`，将其作为中文正文排版的基准。
2. 中文正文默认从 `S_original` 开始排版，不主动缩小。
3. 若中文内容变长，优先采用正文重新流排、对象整体纵向移动、跨栏、跨页或增加页面，而不是缩小字号。
4. 只有在完成上述重排后仍存在局部排版困难时，才允许小幅降低字号。
5. 中文正文原则上不低于 `0.95 × S_original`。
6. 对 A4、Letter 或近似尺寸的普通双栏科技论文，中文正文原则上不得低于 `7.5 pt`；该数值是安全下限，不是目标字号。
7. 若原论文自身字号已经异常小，可根据原页面视觉效果适度提高中文字号，而不是继续机械跟随原字号。
8. 图注、表注和脚注可以小于正文，但必须清晰可读。
9. 不得在页面仍有大量可用空间时继续缩小正文字号。
10. 不得为了保持原页数而牺牲正文字号和可读性。

---

## 七、中文标点和中英文混排

本节在标点、空格和字体归属方面具有高排版优先级，但不得覆盖 P0 内容完整性要求。

### 中文标点

中文句子必须使用中文全角标点：

`，。；：！？、（）“”‘’《》`

不得在普通中文正文中使用对应的半角英文标点：

`, . ; : ? ! ( )`

章节结构编号中的半角句点可以保留，例如：

- `I. 引言`
- `A. 系统架构`

标题内部的标点仍使用中文全角标点，例如：

`A. 高能效数据流：行驻留`

### 中英文之间默认不加空格

正确：

- `Eyeriss芯片`
- `在Eyeriss中`
- `RS数据流`
- `CNN形状`
- `PE阵列`
- `图3`
- `表II`
- `如[12]所示`
- `第5个周期`

错误：

- `Eyeriss 芯片`
- `在 Eyeriss 中`
- `RS 数据流`
- `CNN 形状`
- `PE 阵列`
- `图 3`
- `如 [12] 所示`
- `第 5 个周期`

不得为了在中文字体和 Times New Roman 之间切换而插入空格。

### 允许保留空格的情况

- 数值与单位：`200 MHz`、`65 nm`、`278 mW`
- 数学表达式：`5 × 5`、`N = 4`、`e ≤ E`
- 英文多词名称：`Row Stationary`
- 代码、命令、URL、DOI 和文件名内部

### 中文列举

正确：

`主要包括：1）减少数据搬移；2）利用数据统计特性。`

错误：

`主要包括： 1） 减少数据搬移； 2） 利用数据统计特性。`

### 括号

正确：

`处理单元（Processing Element，PE）`

错误：

`处理单元 (Processing Element, PE)`

公式、代码、函数和原始编号中的半角括号保持原样，例如：

- `ReLU(x)`
- `(1)`
- `O[z][u][x][y]`

---

## 八、不可拆分单元和断行

以下内容必须作为不可拆分单元处理：

- 连续英文字母和英文缩写；
- 专有名称、模型名称和层名称；
- 引用编号和引用范围；
- 数值与单位；
- 数学表达式；
- DOI、URL、代码和文件名。

例如：

- `CNN`
- `FPGA`
- `DRAM`
- `Eyeriss`
- `VGG-16`
- `CONV1–CONV5`
- `[12]–[22]`
- `5 × 5`
- `200 MHz`
- `33.6 GMAC/s`
- `65 nm`

严禁出现：

- `C`与`NN`分处两行；
- `FP`与`GA`分处两行；
- `DR`与`AM`分处两行；
- `VGG-`与`16`分处两行；
- `200`与`MHz`分处两行；
- 引用编号单独占一行；
- 标点单独出现在行首；
- 左括号或左引号孤立在行尾；
- 只有一两个字符的异常短行；
- 因两端对齐产生异常大的空格。

中文两端对齐不得通过拉伸普通空格实现。段落最后一行不得强制拉满。

---

## 九、公式区域冻结

所有数学公式均作为不可修改内容处理，包括：

- 行间公式；
- 行内公式；
- 公式编号；
- 矩阵、分式、根式、积分和求和；
- 上标、下标和希腊字母；
- 变量、运算符和约束条件；
- 公式内部的文字、数字和单位。

严格要求：

1. 公式本体不翻译，不修改数学内容，不改变公式编号。
2. 优先直接复用原 PDF 中的公式对象或原公式区域，保持原始清晰度。
3. 不得对公式进行 OCR 后凭识别结果重新输入或重建。
4. 不得把清晰公式降级为低分辨率截图或模糊位图。
5. 不得裁切、遮挡或有损压缩公式。
6. 只翻译公式前后的解释性正文。
7. 中文正文不得覆盖、遮挡或挤压公式。
8. 行内公式作为不可拆分单元处理。
9. 公式的内容、比例、编号和显示质量应保持不变。
10. 允许公式作为完整对象随中文正文重新流排进行合理的整体纵向移动。
11. 不要求公式保持与原 PDF 完全相同的绝对 y 坐标。
12. 公式应继续位于原章节、原栏或对应说明文字附近，不得跨越不相关章节或破坏阅读顺序。
13. 若当前环境无法直接无损复用原公式对象，应选择能够保持公式内容、编号、比例和清晰度的替代方式，并逐项核对结果。

---

## 十、参考文献区域完全冻结

从 `REFERENCES`、`References` 或 `BIBLIOGRAPHY` 标题开始，到最后一条参考文献结束，参考文献区域必须保持原 PDF 原样。

严格要求：

- 不翻译参考文献标题；
- 不翻译文献条目；
- 不重新输入；
- 不重新断行；
- 不重新编号；
- 不修改作者、标题、期刊、会议、年份、页码、DOI 或 URL；
- 不改变参考文献内部的字体、字号、粗体、斜体、缩进和栏结构；
- 不强制替换为 Times New Roman；
- 不得覆盖、遮挡或裁切；
- 不得让中文正文侵入参考文献区域。

如果参考文献与正文位于同一页，只翻译参考文献冻结区域之外的正文。

参考文献区域的内部排版保持原样；若前文因中文重排导致页数变化，可以在整体文档中相应后移，但不得改变参考文献区域自身的内容与内部结构。

参考文献之后的作者简介、致谢或附录不自动属于参考文献冻结区域，除非另有说明，应正常翻译。

---

## 十一、图片、图表和表格

### 普通图片与数据图表

- 普通图片保持原样。
- 不翻译、覆盖或重绘图片内部文字。
- 不修改图片内容、颜色、清晰度和长宽比例。
- 图片图注需要翻译。
- 数据图表内部英文和标注保持原样，只翻译图注。
- 不得根据文字描述重新生成或重绘原图。
- 图片可以作为完整对象随正文重排进行合理纵向移动。
- 不要求图片保持与原 PDF 完全相同的绝对 y 坐标。
- 图片应继续位于原章节、原栏或对应正文附近。
- 原图片尺寸优先保持；若页面重排确有需要，只允许在不影响清晰度和阅读性的前提下进行轻微等比例调整。

### 表格

可以翻译：

- 表题；
- 表头；
- 行标题；
- 列标题；
- 说明文字。

不得改变：

- 数值；
- 单位；
- 变量；
- 实验结果；
- 数据精度；
- 行列对应关系；
- 合并单元格关系；
- 上下标和脚注标记。

表格不得出现文字越界、数据遮挡或行列错位。

表格可以作为完整对象随中文正文重新排版进行合理纵向移动，但不得破坏其与对应正文的逻辑关系。

---

## 十二、版面重排与空间利用

应保持原论文的整体版式风格，包括：

- 页面尺寸和方向；
- 单栏或双栏结构；
- 栏宽和栏间距；
- 标题层级；
- 页眉、页脚和页码风格；
- 图片、公式和表格与正文之间的逻辑对应关系。

但不得机械保持正文、标题、图片、公式和表格在原 PDF 中的绝对纵向坐标。

英文翻译成中文后，由于文字长度和字体度量会发生变化，必须对中文页面进行自然重新流排。

### 重排原则

1. 保持所有内容的阅读顺序和逻辑对应关系。
2. 原文一个自然段仍对应中文一个自然段。
3. 中文段落的起始和结束纵向位置可以与英文不同。
4. 后续正文应根据前文实际占用高度自然向上或向下流动。
5. 图片、表格和公式可作为完整对象在同一逻辑位置附近适度纵向移动。
6. 不要求图片、表格、公式保持与原 PDF 完全相同的 y 坐标。
7. 优先保持其所在章节、所在栏及前后对应正文关系。
8. 中文明显短于英文时，应通过正常字号和自然流排使用页面空间，不得把正文缩成很小的一块并保留大面积无意义空白。
9. 中文明显长于英文时，应自然流入下一栏或下一页，必要时增加页面，不得首先通过缩小字号解决。
10. 页数不要求与原 PDF 一致，但在不牺牲字号、可读性和版式质量的前提下，应尽量避免无意义增加页数。
11. 若原论文 8 页而中文自然排版为 9 页，应接受；不得为了强行保持 8 页而压缩字号或内容。
12. 若中文内容更紧凑，也不要求通过人为增大段间距来强制维持原页数。

### 版面调整顺序

当中文长度与原英文不同，需要调整版面时，按以下顺序处理：

1. 保持正文完整性和逻辑顺序；
2. 保持清晰可读的中文正文字号；
3. 保持正常、协调的行距；
4. 允许正文自然重新流动；
5. 允许图片、表格、公式作为整体适度纵向移动；
6. 必要时允许内容自然流入下一栏或下一页；
7. 必要时允许增加页面；
8. 最后才考虑小幅调整字号、字距和段间距。

不得首先通过缩小字号解决版面问题。

### 禁止的强行塞版方式

不得通过以下方式强行把中文塞回原英文版面：

- 保留错误硬换行；
- 拆开缩写、引用、数字或单位；
- 极端压缩字距；
- 使用明显过小字号；
- 大幅压缩正常行距；
- 裁切、隐藏或删除内容；
- 让文字覆盖图片、公式、表格或参考文献；
- 将整页栅格化为低清晰度图片；
- 为保持原页数而牺牲可读性。

段落在当前栏放不下时，应自然延续到下一栏或下一页。

---

## 十三、页面视觉密度与空白控制

最终中文 PDF 应保持与原论文相近的整体阅读密度，但不要求逐页填满。

合理的留白可以存在；禁止的是由错误字号或机械固定坐标造成的大面积无意义空白。

逐页检查时应重点判断：

1. 中文正文的视觉大小是否与页面尺寸协调。
2. 中文正文是否明显小于原英文正文。
3. 是否出现“正文很小，但同栏或同页仍有大量未利用空间”的情况。
4. 是否因固定原文本框高度、固定对象坐标或不必要缩小字号而形成大片空白。
5. 是否可以通过恢复合理字号、正常行距、正文重新流排或对象整体移动改善空间利用。
6. 同一栏内若出现明显超过约 3–4 行正文高度的连续空白，应检查其是否有合理的版式原因，例如图片、公式、章节分隔或分页控制。
7. 不得仅为了消灭空白而夸张放大字号、增大段间距、改变图像比例或制造松散排版。

若同时出现以下两项：

- 中文正文字号明显偏小；
- 页面或栏内仍存在大量可用空间；

则应视为排版异常，优先增大或恢复正文字号并重新流排，而不是保留当前结果。

视觉检查只用于发现异常，不用于替代字号的确定性规则。

---

## 十四、必须执行的处理流程

1. 分析原 PDF 的页面尺寸、栏结构、正文主字号和阅读顺序。
2. 识别标题、作者与单位、正文栏、自然段、列表、公式、图片、表格、图注、脚注和参考文献。
3. 根据页面坐标和真实版式重建英文自然段。
4. 修复英文行尾断词。
5. 检查跨栏、跨页段落续接。
6. 建立并统一全文术语表。
7. 以完整自然段为单位翻译。
8. 对公式和参考文献区域设置冻结保护。
9. 按规则翻译首页元信息。
10. 应用中文字体、Times New Roman、中文标点和中英文空格规则。
11. 以原正文主字号为基准排版中文正文，不主动缩小。
12. 对中文正文、图片、表格和公式进行自然流排，允许合理纵向移动和跨页。
13. 在保证可读性的前提下控制页数，避免无意义增加页面。
14. 生成可选择、复制和检索的中文 PDF。
15. 将输出 PDF 的全部页面渲染为高分辨率图片。
16. 逐页与原 PDF 对照检查。
17. 执行自动异常检测。
18. 发现问题后修改、重新生成并重新检查，直到满足完成条件。

---

## 十五、逐页视觉检查与自动异常检测

必须检查最终渲染出来的真实页面，而不是只检查 PDF 是否能打开或程序是否报错。

每一页至少检查：

- 自然段数量、顺序和边界是否正确；
- 是否错误继承英文视觉行尾；
- 是否存在句中硬换行、异常短行或孤立标点；
- 跨栏、跨页段落是否正确连续；
- 是否存在正文遗漏、重复、顺序错误或未翻译正文；
- 缩写、专有名称、引用、数字和单位是否断裂；
- 中文标点和中英文空格是否符合规则；
- 是否因两端对齐产生异常大空白；
- 中文正文实际字号是否符合字号规则；
- 是否存在正文很小但页面仍有大量无意义空白；
- 是否存在文字裁切、缺失、溢出或覆盖；
- 图片内容、比例、颜色和清晰度是否保持；
- 图片与对应正文的逻辑位置是否正确；
- 表格数据、行列对应和精度是否保持不变；
- 公式内容、编号、比例和清晰度是否保持；
- 公式是否被错误 OCR 重建、模糊化、裁切或遮挡；
- 参考文献区域是否完整保持原样；
- 中文正文是否可选择、复制和检索；
- 实际字体是否正确并已嵌入或子集嵌入；
- 页数增加是否有合理原因，是否存在无意义松散分页。

自动检测至少包括：

1. 缩写跨行断裂检查；
2. 数值与单位断裂检查；
3. 引用编号断裂检查；
4. 异常短行和孤立标点检查；
5. 中文正文半角标点检查；
6. 中英文、数字和引用之间的异常空格检查；
7. 自然段数量和顺序检查；
8. 正文遗漏、重复和未翻译内容检查；
9. 文本超出栏或页面边界检查；
10. 文字与图片、公式、表格、页眉、页脚及参考文献重叠检查；
11. 实际字体、字体嵌入和正文最小字号检查；
12. 中文可选择、复制和检索检查；
13. 全文术语一致性检查；
14. 公式清晰度与原 PDF 对比检查；
15. 页面空白与正文视觉密度异常检查。

---

## 十六、完成条件

只有同时满足以下条件，才能提交最终中文 PDF：

- 所有应翻译正文均已完整翻译；
- 原文与译文自然段一一对应；
- 中文段内自然换行；
- 不存在英文视觉行尾残留；
- 不存在错误硬换行、异常短行和异常空格；
- 不存在缩写、引用、数字或单位断裂；
- 不存在文字裁切、缺失、溢出或遮挡；
- 跨栏和跨页段落连续正确；
- 首页元信息按规则正确处理；
- 图片保持原内容、比例、颜色和清晰度；
- 表格和图表数据未改变；
- 公式内容、编号、比例和清晰度保持不变；
- 参考文献区域保持原 PDF 原样；
- 中文正文实际字号符合字号规则，没有不必要缩小；
- 不存在“正文很小但页面仍有大量无意义空白”的明显排版异常；
- 页面和栏内留白具有合理版式原因；
- 页数变化合理，没有为了保持原页数而压缩字号，也没有无意义增加页数；
- 中文标点和中英文混排符合要求；
- 中文正文可选择、复制和检索；
- 所有页面均已实际渲染并逐页检查；
- 视觉检查和自动检测发现的问题均已修复并复查。

若某项技术要求在当前环境中客观无法无损实现，应优先保证内容正确、公式和数据正确、正文可读以及 PDF 可正常阅读；不得为了表面满足版式要求而破坏更高优先级目标。
